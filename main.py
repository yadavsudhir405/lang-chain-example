from source import SampleReader

if __name__ == "__main__":
    # SampleReader().run()
    # SampleReader().split_chunks_using_character_text_splitter()
    # SampleReader().split_text_using_recursive_character_text_splitter()
    SampleReader().embeddings()
